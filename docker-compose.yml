version: '3'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GOOGLE_GENERATIVE_AI_API_KEY=${GOOGLE_GENERATIVE_AI_API_KEY}
      - DATABASE_URL=file:/app/db/prod.db
    volumes:
      - ./db:/app/db
